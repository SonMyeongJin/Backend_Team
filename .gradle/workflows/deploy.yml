name: CI/CD Pipeline

on:
  push:
    branches:
      - develop
      - 3-be-cicd-구축
  pull_request:
    branches:
      - develop
      - 3-be-cicd-구축

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

  deploy:
    if: github.ref == 'refs/heads/deploy'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy
        run: |
          echo "Deploying to production..."
          # 여기에 배포 스크립트 또는 명령어를 추가합니다.